interface IParts {
    id: number,
    code: string,
    description: string,
    unit: string,
    quantity: number,
    quantityMin: number,
    quantityMax: number,
    quantitySec: number

}

export type { IParts }